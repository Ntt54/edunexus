# ollama-tutor

Local AI tutor: a document library with spaced-repetition, exercises, and RAG
over your books. CPU-friendly, Ollama-backed web GUI.

Run: `ollama-tutor-webgui --host 127.0.0.1 --port 8080`
Tests: `pytest tests/ -q`
